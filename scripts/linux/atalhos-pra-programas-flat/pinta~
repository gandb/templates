/*Este script deve ser posto na pasta /usr/local/bin para ser executado globalmente ou em ~/local/bin se for pra executar apenas pro usuário. */
#!/bin/bash
/usr/bin/flatpak run --branch=stable --arch=x86_64 --command=pinta --file-forwarding com.github.PintaProject.Pinta @@ "$@" @@

