import 'package:flutter/material.dart';

class EListMenu extends StatefulWidget {
  const EListMenu({super.key});

  @override
  State<EListMenu> createState() => _EListMenu();
}

class _EListMenu extends State<EListMenu> {
  //recebe uma lista de fooditem e ele gera uma lista do menu item
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: const [],
    );
  }
}
